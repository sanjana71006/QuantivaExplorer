MONGODB_URI=your-mongodb-uri
GEMINI_API_KEY=your-gemini-api-key
# Used for login/signup token signing (set this in Render/production)
JWT_SECRET=your-long-random-secret
# Optional (default: 7d)
# JWT_EXPIRES_IN=7d
# Example optional variables
# PORT=8080
